import * as React from 'react';
import { Component } from 'react';
import './header.scss'

class Header extends Component {
  render(): React.ReactNode {
    return <header id="Top" className='header'></header>;
  }
}

export default Header;
