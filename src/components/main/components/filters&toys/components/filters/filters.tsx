import * as React from 'react';
import { Component } from 'react';


type Props = {
  toysData: object
}

export default function Filters({ toysData }: Props) {
  return (
    <div>

    </div>
  );
}